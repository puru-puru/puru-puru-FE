import styled from "styled-components";

export const CompleteToggle = styled.div`
    display: flex;
    align-items: center;
    justify-content: center;

    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 310px;
    height: 62px;
    z-index: 1;

    background: #ffffff;
    border: 1px solid #cccccc;
    box-shadow: 0px 0px 40px rgba(0, 0, 0, 0.35);
    border-radius: 10px;
`;
